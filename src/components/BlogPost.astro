---
import ImageCard from "./ImageCard.astro";

type Props = {
  title: string;
  url: string;
  img?: string;
};

const { title, url, img } = Astro.props;
---

<ImageCard title={title} url={url} img={img} />
